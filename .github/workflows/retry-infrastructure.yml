name: Retry CI infrastructure failures

# This controller inspects run metadata and logs; it never executes the failed ref.
on: # zizmor: ignore[dangerous-triggers]
  workflow_run:
    workflows: [Tests]
    types: [completed]

permissions: {}

jobs:
  retry:
    if: >-
      ${{ github.event.workflow_run.conclusion == 'failure' &&
      github.event.workflow_run.run_attempt == 1 }}
    runs-on: ubuntu-24.04
    permissions:
      actions: write
      contents: read
    steps:
      - uses: cgraf78/actions/.github/actions/infra-retry@023a7d521187d9a1dc281a4b2beddbf03845d689
        with:
          github-token: ${{ github.token }}
          repository: ${{ github.repository }}
          run-id: ${{ github.event.workflow_run.id }}
          run-attempt: ${{ github.event.workflow_run.run_attempt }}
          conclusion: ${{ github.event.workflow_run.conclusion }}
